# agent-shell-fork-tree：缓存与增量重建设计

## 目标

重建当前会话及相关分支的历史树，支持插件安装前或外部创建的会话。

**首次发现从头识别；已有缓存从各自的缓存终点续建。** 不把所有项目会话挂在一起，也不让会话之间两两比较。

## 节点匹配

每个节点表示一轮对话，只比较用户输入与助手回复的文本。工具活动用于预览，不参与匹配。

哈希函数只接收两个参数：

- **用户输入文本**：本轮用户提交的正文。
- **助手回复文本**：本轮对用户可见的回复正文，不包含思考过程和工具输出。

```text
轮次标识 = 用户、助手消息 ID 的有序组合
文本指纹 = 哈希（用户输入文本，助手回复文本）

在相同父节点下：
    优先通过可复用的轮次标识查找
    如果未命中：
        复用已缓存的文本指纹；没有缓存才计算
        通过文本指纹查找
    如果仍未命中：
        创建节点
```

两个参数按固定顺序、保留边界参与计算，避免直接拼接导致内容歧义。消息 ID、会话 ID、父节点、时间和其他元数据都不加入哈希；父节点只限定节点查找范围。

同一轮未变化的正文只计算一次指纹；ID 已命中时不额外计算。

只合并至少包含一个完整轮次的共同前缀，空会话只显示自身。分叉后出现相同文字，也不跨路径合并；历史完全相同的会话，仍保留独立的会话入口。

本次实测中，Codex 的继承消息 ID 可以跨 fork 复用；OpenCode 的 fork 会重新编号，首次通过文本指纹匹配。同一会话的后续加载，两者都可以利用稳定 ID 定位历史。这些是已验证版本的行为，不是对所有 ACP 后端的跨会话 ID 保证。

## 缓存内容

缓存跨打开和重启保留，每个会话单独记录：

- 是否已完成同步，以及上次确认的更新时间。
- 首轮所属的对话组，包括已经确认不相关的结果。
- 已建立的历史路径、消息 ID 对应关系和已计算的文本指纹。
- **最后一个已完成并确认的历史节点，作为下次续建的终点。**

已确认未变化的历史直接复用指纹。每处理完一个完整轮次就推进缓存终点；未完成的回答不推进终点。只有完成同步才确认更新时间，中途退出后继续未完成的工作。

本地保存的 fork 关系可以帮助直接复用前缀，但不是重建的前提。

## 自动与手动重建

自动重建可以开启或关闭；无论开关状态如何，都可以按 `g` 手动重建。

| 方式 | 触发时机 |
| --- | --- |
| 自动重建开启 | 打开树时，以及树显示期间当前会话完成一轮或创建 fork 后 |
| 自动重建关闭 | 打开树只展示缓存，按 `g` 才更新 |
| 手动重建 `g` | 立即检查新会话和已有会话的变化，并从缓存终点续建 |

没有缓存且关闭自动重建时，提示按 `g` 首次建树。**自动重建和 `g` 都复用缓存，不清空已有索引。**

```text
打开历史树：
    读取缓存，立即展示已有结果
    如果开启自动重建：
        执行增量重建

树显示期间，当前会话完成一轮或创建 fork：
    如果开启自动重建：
        执行增量重建

用户按 g：
    执行增量重建

增量重建：
    更新当前会话
    获取同项目的候选会话列表
    只安排新会话、发生变化或无法确认是否变化的会话

    逐个更新候选：
        属于当前对话组的分支，加入当前树
        不相关的候选，只保存分类结果
        逐步更新界面和发现进度
```

优先更新当前会话和已知分支，再处理未知候选。标题、时间可以影响处理顺序，但不能作为排除分支的依据。

每次重建仍检查会话列表，以发现新 fork；续建位置指的是每个会话内部的历史位置。

## 从缓存终点续建

```text
更新会话（会话）：
    如果能确认会话没有变化：
        直接复用缓存结果
        结束

    取得会话历史

    如果已有历史路径缓存：
        定位并确认这个会话的缓存终点
        如果无法确认：
            报告增量重建失败，进入失败处理流程
            结束
        当前位置 = 缓存终点
        待处理历史 = 终点之后的完整轮次
    否则：
        用首轮的 ID 或文本指纹确定所属对话组
        如果与当前对话不相关：
            保存分类结果，结束
        当前位置 = 该对话组的根节点
        待处理历史 = 全部完整轮次

    对待处理历史中的每一轮：
        在当前位置下，按 ID 或文本指纹复用或创建节点
        记录该会话与节点的对应关系
        前进到该节点，并推进缓存终点

    更新该会话的独立入口和更新时间
```

缓存终点通过消息 ID 定位；不能使用 ID 时，按缓存路径中的轮次指纹序列确认位置，所需的指纹计算计入定位成本。定位终点后，前面的历史直接复用，不再次逐轮比较或建树。

历史仅追加时走续建流程。历史被改写、压缩，导致缓存前缀失效或终点无法对应时，按增量重建失败处理，不自动从头重建。新发现、尚无缓存的会话仍正常从头识别。

## 增量失败与全量重建

自动重建和按 `g` 手动重建使用相同的失败处理流程。

```text
增量重建失败：
    停止本次重建，保留已经确认的树和缓存进度
    提醒用户，并显示失败会话及原因
    询问：“增量重建失败，是否全量重建？”

    用户同意：
        重新读取项目候选历史，不跳过已有缓存部分
        从头识别当前会话的相关分支，建立新的树和索引
        成功后替换当前树和对应缓存

    用户拒绝或取消：
        保留现有结果，之后仍可按 g 重试增量重建
```

每次失败只询问一次；未确认时不执行全量重建。全量重建若仍失败，显示原因并保留现有结果，不自动重复重试。

## 成本与读取边界

| 情况 | 工作量 |
| --- | --- |
| 会话未变化 | 复用缓存，不加载历史、不比较内容 |
| 会话增加了新轮次 | 定位缓存终点，只匹配后续轮次 |
| 新发现的 fork | 首次识别共同前缀，随后保存自己的缓存终点 |
| 不相关的候选 | 首轮排除后保存结果，不为当前树比较后续轮次 |
| 用户确认全量重建 | 重新读取候选历史，从头匹配并建立索引 |

使用父节点下的 ID 或文本指纹索引查找节点，避免遍历其他会话。定位缓存终点后，续建成本预期与**新增轮次数和新增指纹涉及的文本长度**成正比。

本地续建与后端读取是两件事：后端支持按位置读取时只取新增历史；只支持完整回放时，仍需接收旧历史并定位缓存终点，但不重复建树。首次完整发现未知分支，最坏仍需读取每个候选一次。

## 使用边界

历史节点可以预览、搜索、标记和查看差异；只有真实会话入口可以继续或 fork。fork 使用后端会话的当前末尾，不回滚文件或 Git 状态。

仅凭共同内容重建的是共同历史树，不保证还原真实的 fork 父子关系。全部流程使用通用 ACP 能力，不读取供应商私有 parent 字段。

使用与测试方法见 [README.md](README.md)。
